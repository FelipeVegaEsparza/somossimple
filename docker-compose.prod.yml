# Despliegue en Dokploy (tipo Docker Compose, branch main, Compose path:
# docker-compose.prod.yml).
#
# Servicios:
#   web        → nginx + php-fpm (aquí apuntas el dominio, puerto 80)
#   worker     → colas (queue:work)
#   scheduler  → tareas programadas (schedule:run)
#   mysql      → base de datos incluida (misma red, sin problemas de DNS)
#
# Variables: define en el panel de Dokploy (Environment) al menos
#   APP_KEY, APP_URL, DB_PASSWORD (opcional) y las que necesites.
# Las demás tienen valores por defecto abajo.

x-app-env: &app-env
    APP_NAME: ${APP_NAME:-SomosSimple}
    APP_ENV: ${APP_ENV:-production}
    APP_KEY: ${APP_KEY}
    APP_DEBUG: ${APP_DEBUG:-false}
    APP_URL: ${APP_URL}
    APP_LOCALE: ${APP_LOCALE:-es}
    APP_FALLBACK_LOCALE: ${APP_FALLBACK_LOCALE:-en}
    APP_TIMEZONE: ${APP_TIMEZONE:-America/Santiago}

    LOG_CHANNEL: ${LOG_CHANNEL:-stderr}
    LOG_LEVEL: ${LOG_LEVEL:-warning}

    # Apuntan al servicio mysql de este mismo compose.
    DB_CONNECTION: mysql
    DB_HOST: ${DB_HOST:-mysql}
    DB_PORT: 3306
    DB_DATABASE: ${DB_DATABASE:-simpledb}
    DB_USERNAME: ${DB_USERNAME:-simple}
    DB_PASSWORD: ${DB_PASSWORD:-simple_secret}

    SESSION_DRIVER: ${SESSION_DRIVER:-database}
    SESSION_LIFETIME: ${SESSION_LIFETIME:-120}
    SESSION_SECURE_COOKIE: ${SESSION_SECURE_COOKIE:-true}
    SESSION_ENCRYPT: ${SESSION_ENCRYPT:-false}

    BROADCAST_CONNECTION: ${BROADCAST_CONNECTION:-log}
    FILESYSTEM_DISK: ${FILESYSTEM_DISK:-local}
    QUEUE_CONNECTION: ${QUEUE_CONNECTION:-database}
    CACHE_STORE: ${CACHE_STORE:-database}

    MAIL_MAILER: ${MAIL_MAILER:-log}
    MAIL_FROM_ADDRESS: ${MAIL_FROM_ADDRESS:-hola@somossimple.cl}
    MAIL_FROM_NAME: ${MAIL_FROM_NAME:-SomosSimple}

    LOYALTY_APPLE_WALLET_ENABLED: ${LOYALTY_APPLE_WALLET_ENABLED:-false}
    LOYALTY_GOOGLE_WALLET_ENABLED: ${LOYALTY_GOOGLE_WALLET_ENABLED:-false}

    TICKETERA_PAYMENT_PROVIDER: ${TICKETERA_PAYMENT_PROVIDER:-manual}
    TICKETERA_WEBPAY_ENABLED: ${TICKETERA_WEBPAY_ENABLED:-false}
    TICKETERA_MERCADOPAGO_ENABLED: ${TICKETERA_MERCADOPAGO_ENABLED:-false}
    TICKETERA_FLOW_ENABLED: ${TICKETERA_FLOW_ENABLED:-false}
    TICKETERA_APPLE_WALLET_ENABLED: ${TICKETERA_APPLE_WALLET_ENABLED:-false}
    TICKETERA_GOOGLE_WALLET_ENABLED: ${TICKETERA_GOOGLE_WALLET_ENABLED:-false}

x-app: &app
    build:
        context: .
        dockerfile: Dockerfile.prod
    restart: unless-stopped
    environment:
        <<: *app-env
    volumes:
        # Persistencia de subidas (logos, portadas, imágenes de módulos).
        - storage:/var/www/html/storage/app/public
    depends_on:
        mysql:
            condition: service_healthy
    networks:
        - dokploy-network

services:
    web:
        <<: *app
        command: ["web"]
        expose:
            - "80"
        # En Dokploy: agrega el dominio apuntando a este servicio, puerto 80.

    worker:
        <<: *app
        command: ["worker"]

    scheduler:
        <<: *app
        command: ["scheduler"]

    mysql:
        image: mysql:8.0
        restart: unless-stopped
        networks:
            - dokploy-network
        environment:
            MYSQL_DATABASE: ${DB_DATABASE:-simpledb}
            MYSQL_USER: ${DB_USERNAME:-simple}
            MYSQL_PASSWORD: ${DB_PASSWORD:-simple_secret}
            MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:-root_secret}
        volumes:
            - mysql:/var/lib/mysql
        healthcheck:
            test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u${DB_USERNAME:-simple}", "-p${DB_PASSWORD:-simple_secret}"]
            interval: 5s
            timeout: 5s
            retries: 30
        # No se expone al exterior; solo lo usa la app por la red interna.

volumes:
    storage:
    mysql:

networks:
    # Red de Dokploy/Traefik. Todos los servicios van aquí (DNS entre contenedores).
    dokploy-network:
        external: true
